# Simple Calculator in C++

This is a basic console-based calculator program written in C++. It takes user input for name, two integers, and an arithmetic operation (`+`, `-`, `*`, `/`). Then it calculates and displays the result.

---

## 🧮 Features

- Greets the user by name.
- Accepts two integer numbers.
- Performs one of the four basic arithmetic operations:
  - Addition (`+`)
  - Subtraction (`-`)
  - Multiplication (`*`)
  - Division (`/`)
- Handles division by zero with an error message.
- Handles invalid operators.

---

## 📌 Example Usage

```text
enter your name : Ahmed
enter your first number : 10
enter your first number : 5
hello Ahmed enter your operation (* , / , + , -)
+
the result of sum is 15
